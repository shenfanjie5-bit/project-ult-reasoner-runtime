examples:
  none:
    callback:
      backend: none
      enabled: false
      endpoint: null
  otel:
    callback:
      backend: otel
      enabled: true
      endpoint: http://localhost:4318/v1/traces
  langfuse:
    callback:
      backend: langfuse
      enabled: true
      endpoint: http://localhost:3000
